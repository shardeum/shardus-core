// This is here only to test the test running setup and can be safely
// removed once more tests are added.

const test = require('tap').test

test('runs the integration files', t => {
  t.equal(true, true, 'runs the integration files')
  t.end()
})
