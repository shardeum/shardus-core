{
  "name": "shardus-enterprise-server",
  "version": "0.0.0",
  "description": "The shardus enterprise server",
  "main": "index.js",
  "scripts": {
    "test": "tap test/**/*.spec.js",
    "test:unit": "tap test/unit/**/*.spec.js unit",
    "test:integration": "tap test/integration/**/*.spec.js unit"
  },
  "repository": {
    "type": "git",
    "url": "git@gitlab.com:shardus/enterprise/shardus-enterprise-server.git"
  },
  "devDependencies": {
    "tap": "^12.0.1"
  },
  "dependencies": {
    "sqlite": "^3.0.0"
  }
}
