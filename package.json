{
  "name": "shardus-enterprise-server",
  "version": "0.0.0",
  "description": "The shardus enterprise server",
  "main": "index.js",
  "scripts": {
    "test:unit": "echo \"Error: still determining framework to use!\" && exit 1",
    "test:integration": "echo \"Error: still determining framework to use!\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@gitlab.com:shardus/enterprise/shardus-enterprise-server.git"
  }
}
