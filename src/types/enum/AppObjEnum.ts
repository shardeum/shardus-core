/*
 * Enum identifiers for App defined objects.
 * This enum is used to map the App object with its binary serializer and deserializer.
 * @enum {string}
 */
export enum AppObjEnum {
  AppData = 'AppData',
  CachedAppData = 'CachedAppData',
}
